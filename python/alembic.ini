[alembic]
# location of migration scripts
script_location = alembic
# Use DATABASE_URL environment variable if set; CI and startup use this to provide the real DB URL.
# Example: postgresql+asyncpg://postgres:postgres@db:5432/test_db
sqlalchemy.url = ${DATABASE_URL}
