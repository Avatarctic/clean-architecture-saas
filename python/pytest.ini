[pytest]
# Let pytest-asyncio (or other async plugin) pick up coroutine tests automatically
asyncio_mode = auto

# Tests live in the tests/ folder and follow the test_*.py naming convention
testpaths = tests
python_files = test_*.py
# use a project-local basetemp to avoid Windows TMP permission issues
addopts = --basetemp=.pytest_temp
